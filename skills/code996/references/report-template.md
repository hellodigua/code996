# 团队工作强度分析报告

> 生成时间：{日期} · 分析仓库：{单仓库使用 meta.repos[0]；多仓库列出每份结果的 meta.repos[0]} · 分析区间：{meta.since → meta.until；两者缺失时写“全部历史”}

---

## 一、概览与核心结论

**一句话定性**：{根据 index996 和 rating 给出直接判断，例如：「该团队处于重度加班状态，工作强度已超出健康基准」}

| 指标         | 数值                                                   |
| ------------ | ------------------------------------------------------ |
| 996 指数     | {core.index996}（{core.rating}）                       |
| 加班比例     | {core.overTimeRatio}%                                  |
| 总提交数     | {core.totalCommits}                                    |
| 推测上班时间 | {将 workTime.startHour 换算为 HH:MM，例如 9.5 → 09:30} |
| 推测下班时间 | {将 workTime.endHour 换算为 HH:MM，例如 18.5 → 18:30}  |

---

## 二、工作时间分布

### 每日提交时段

{基于 hourlyDistribution，用文字描述提交高峰时段，例如：「提交主要集中在 10:00–12:00 和 14:00–18:00，但 21:00–23:00 仍有明显活跃」}

### 工作日 vs 周末

{基于 weekdayDistribution，描述工作日和周末的提交分布比例}

---

## 三、加班分析

### 工作日加班

{基于 weekdayOvertime，描述哪几天加班最严重，peakDay 是哪天}

### 周末加班

- 周六加班天数（中国节假日模式下还包含工作日法定节假日）：{weekendOvertime.saturdayDays} 天
- 周日加班天数：{weekendOvertime.sundayDays} 天
- 其中真正加班（一天内提交覆盖 ≥3 个不同小时段）：{weekendOvertime.realOvertimeDays} 天

### 深夜加班

- 深夜加班比例：{lateNight.midnightRate}%
- 深夜加班：{lateNight.midnight} 天（按每日最晚提交统计）

### 加班在干什么

{基于 git log 语义分析结果，描述各类加班内容的占比，并列举典型 commit message}

| 类型              | 占比 | 典型示例 |
| ----------------- | ---- | -------- |
| 赶需求 / 功能开发 | {%}  | {示例}   |
| 修线上 Bug        | {%}  | {示例}   |
| 重构 / 技术债     | {%}  | {示例}   |
| CI/CD / 发版      | {%}  | {示例}   |

---

## 四、贡献者 / 人均对比

{基于 team.contributors[]，描述各人加班情况，重点标注加班比例最高的成员}

| 贡献者   | 提交数         | 提交占比            | 加班提交                      | 加班比例                                                          | 强度等级         |
| -------- | -------------- | ------------------- | ----------------------------- | ----------------------------------------------------------------- | ---------------- |
| {author} | {totalCommits} | {commitPercentage}% | {overtimeStats.totalOvertime} | {overtimeStats.totalOvertime / totalCommits × 100，保留一位小数}% | {intensityLevel} |

{如果 overtimeStats 或 intensityLevel 缺失，对应列显示“—”，不要推断或编造}

{如果存在个别成员加班比例远高于团队均值，重点提示风险}

---

## 五、多仓库聚合视图（若适用）

{遍历步骤 2 独立收集的每份单仓库 JSON，使用各自的 meta.repos[0] 和 core 字段进行横向对比；不要读取恒为 null 的 multiRepo}

| 仓库            | 996 指数        | 加班比例              | 提交数              |
| --------------- | --------------- | --------------------- | ------------------- |
| {meta.repos[0]} | {core.index996} | {core.overTimeRatio}% | {core.totalCommits} |

---

## 六、风险评估与改进建议

### 风险评估

{结合用户口述背景（团队规模、弹性制、约定工时）与上述数据，给出综合判断。例如：「按约定 8h 工时计算，实际工作跨度达 X 小时，超出 Y%」}

### 改进建议

1. {具体建议，例如：「控制周五上线频率，减少周末紧急修复」}
2. {具体建议}
3. {具体建议}
4. {具体建议（如有）}
5. {具体建议（如有）}
